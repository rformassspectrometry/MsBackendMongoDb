% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MsBackendMongoDB-functions.R
\name{start_mongodb}
\alias{start_mongodb}
\alias{stop_mongodb}
\title{Start and stop MongoDB instances}
\usage{
start_mongodb(
  cmd = "mongod",
  dbpath = file.path(tempdir(), "mongo"),
  logpath = file.path(tempdir(), "mongo.log")
)

stop_mongodb(cmd = "mongod", dbpath = file.path(tempdir(), "mongo"))
}
\arguments{
\item{cmd}{\code{character(1)} with the binary to start or stop the MongoDB.}

\item{dbpath}{\code{character(1)} with the location of the MongoDB database.
Defaults to \code{file.path(tempdir(), "mongo")} hence using a temporary
folder for the data.}

\item{logpath}{\code{character(1)} with the location of the log file for the
database. Defaults to \code{file.path(tempdir(), "mongo.log")}.}
}
\value{
\code{start_mongodb()} and \code{stop_mongodb()} don't return any value, but
throw an error if the \code{system2} call did not return \code{0}.
}
\description{
The \code{start_mongodb()} and \code{stop_mongodb()} functions start and stop a
MongoDB database using the \emph{mongod} system call. These functions require
an installation of the MongoDB server and the \emph{mongod} binary to be present
in the System path.
}
\examples{

## Start a MongoDB service with the database location in *test_mongo* in
## the System's temporary folder.
start_mongodb(dbpath = file.path(tempdir(), "test_mongo"))

## Stop the MongoDB
stop_mongodb(dbpath = file.path(tempdir(), "test_mongo"))

## Remove the database
unlink(file.path(tempdir(), "test_mongo"), recursive = TRUE)
}
