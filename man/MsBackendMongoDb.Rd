% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MsBackendMongoDB.R
\name{MsBackendMongoDb}
\alias{MsBackendMongoDb}
\alias{MsBackendMongoDb-class}
\alias{backendInitialize,MsBackendMongoDb-method}
\alias{dataStorage,MsBackendMongoDb-method}
\alias{peaksData,MsBackendMongoDb-method}
\alias{peaksVariables,MsBackendMongoDb-method}
\alias{intensity,MsBackendMongoDb-method}
\alias{intensity<-,MsBackendMongoDb-method}
\alias{mz,MsBackendMongoDb-method}
\alias{mz<-,MsBackendMongoDb-method}
\alias{spectraData,MsBackendMongoDb-method}
\alias{reset,MsBackendMongoDb-method}
\alias{spectraNames,MsBackendMongoDb-method}
\alias{spectraNames<-,MsBackendMongoDb-method}
\alias{[,MsBackendMongoDb-method}
\alias{extractByIndex,MsBackendMongoDb-method}
\alias{show,MsBackendMongoDb-method}
\alias{extractByIndex,MsBackendMongoDb,ANY-method}
\title{\code{Spectra} MS backend storing data in a MongoDb database}
\usage{
\S4method{show}{MsBackendMongoDb}(object)

\S4method{backendInitialize}{MsBackendMongoDb}(object, dbcon, data, ...)

\S4method{dataStorage}{MsBackendMongoDb}(object)

\S4method{peaksData}{MsBackendMongoDb}(object, columns = c("mz", "intensity"))

\S4method{peaksVariables}{MsBackendMongoDb}(object)

\S4method{mz}{MsBackendMongoDb}(object)

\S4method{mz}{MsBackendMongoDb}(object) <- value

\S4method{intensity}{MsBackendMongoDb}(object)

\S4method{intensity}{MsBackendMongoDb}(object) <- value

\S4method{spectraNames}{MsBackendMongoDb}(object)

\S4method{spectraNames}{MsBackendMongoDb}(object) <- value

\S4method{spectraData}{MsBackendMongoDb}(object, columns = NULL)

\S4method{[}{MsBackendMongoDb}(x, i, j, ..., drop = FALSE)

\S4method{extractByIndex}{MsBackendMongoDb,ANY}(object, i)

\S4method{reset}{MsBackendMongoDb}(object)
}
\arguments{
\item{object}{A \code{MsBackendMongoDb} instance}

\item{dbcon}{\code{list()} of MongoDb collections, e.g.,
\code{list(compounds_info_coll = mongo_conn)}}

\item{data}{Optional \code{DataFrame} with data to insert into MongoDb}

\item{...}{Additional arguments passed internally}

\item{columns}{\code{Character()} vector of columns to fetch for \code{peaksData} or
\code{spectraData}}

\item{value}{Replacement values for setter functions}

\item{x}{for \verb{[i]}: a \code{MsBackendMongoDb} instance}

\item{i}{\code{integer()} Indices to subset the backend}

\item{j}{\code{integer()} Ignored.}

\item{drop}{\code{logical(1)}, ignored for subsetting}
}
\value{
Depends on the method. See individual function documentation.
}
\description{
\code{MsBackendMongoDb} is an implementation of the \code{\link[Spectra:MsBackend]{Spectra::MsBackend()}} class
for \code{\link[Spectra:Spectra]{Spectra::Spectra()}} objects, storing and retrieving MS data from a
MongoDb database.

This backend allows read-only access to spectra and peaks stored in MongoDb,
but supports temporary modifications of spectra variables via caching.
}
\note{
\code{MsBackendMongoDb} keeps a connection to a MongoDb collection(s) and fetches
spectra and peaks data on demand. By storing only the primary keys (\code{spectrum_id})
in memory, it ensures minimal memory usage, while peaks and metadata are fetched
dynamically.

The backend inherits from \code{\link[Spectra:MsBackendCached]{Spectra::MsBackendCached()}} and supports temporary
modification of spectra variables via the \verb{$<-} operator. Original data in the
database cannot be changed (intensities and m/z values are read-only).
}
\section{Creation of backend objects}{


New backend objects can be created using:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{backend <- MsBackendMongoDb(dbcon = list(compounds_info_coll = mongo_connection))
backend <- backendInitialize(backend, dbcon = backend@dbcon)
}\if{html}{\out{</div>}}
\itemize{
\item \code{dbcon}: A list of MongoDb collections (e.g. from \code{mongolite::mongo()}).
\item \code{backendInitialize()}: populates the object with spectrum IDs and caches
metadata. If \code{data} is provided, it can insert data into the collection.
}
}

\section{Accessing data}{

\itemize{
\item \code{peaksData(object, columns = c("mz", "intensity"))}: returns a \code{list} of
matrices containing the peak data for each spectrum.
\item \code{peaksVariables(object)}: returns available columns for \code{peaksData}.
\item \code{intensity(object)}, \code{mz(object)}: return \code{SimpleList} objects with numeric
intensity or m/z values for each spectrum.
\item \code{spectraData(object, columns)}: returns a \code{DataFrame} with requested spectrum
metadata columns.
\item \code{spectraNames(object)}: returns \code{spectrum_id} values as character.
}
}

\section{Read-only data}{

\itemize{
\item \verb{intensity<-} and \verb{mz<-} are \strong{not supported}.
\item \verb{spectraNames<-} is \strong{not supported}.
}
}

\section{Subsetting and extraction}{

\itemize{
\item \verb{[i]} and \code{extractByIndex(object, i)} subset the backend by spectrum indices.
Original data in the database is never changed.
\item \code{reset(object)} restores the backend to its original state, removing
subsetting and cached variables.
}
}

\section{Miscellaneous}{

\itemize{
\item \code{dataStorage(object)} returns a string describing the MongoDb collections
used by the backend.
}
}

\section{Implementation notes}{


Internally, the backend stores:
\itemize{
\item \code{spectraIds}: integer vector of spectrum IDs
\item \code{dbcon}: MongoDb connection(s)
\item \code{.collections}: list of collection names
\item \code{peak_fun}: function used to fetch peak data
\item \code{localData}: cached spectrum variables
\item \code{nspectra}: total number of spectra
}

Data access functions use \code{peak_fun} to fetch peaks on demand.
}

\author{
Ahlam Mentag
}
